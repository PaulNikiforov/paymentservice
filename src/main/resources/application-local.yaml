server:
  port: 8083

spring:
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/paymentdb}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

payment:
  external-api:
    url: ${EXTERNAL_PAYMENT_API_URL:https://www.random.org/integers}
